# SQL and Data Management Practice

This repository tracks my learning process with SQL and data management foundations.

## Current Contents

- `commands.sql`: SQL practice commands, including table creation examples.
- `database.db`: local SQLite database file used for experimentation.
- `scripts.py`: Python practice script that runs a SQL query and prints book title/category results.

## Goal

Build a consistent foundation in:

- SQL syntax and query writing
- Table design and schema management
- Practical data handling workflows

## Update (March 26, 2026)

Added a new Python practice script to reinforce SQL + application integration concepts:

- Query execution from Python using a database cursor
- Filtering data (`Price > 20.00`)
- Reading and printing returned rows (`Title`, `Category`)
